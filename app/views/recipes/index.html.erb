<%= render 'shared/page-title', title: "List of all Recipes" %>

<div align="center">
	<%= will_paginate @recipes %>
</div>

<%= render 'recipe' , obj: @recipes %>

<div align="center">
	<%= will_paginate @recipes %>
</div>
